streamlit>=1.33.0,<2.0.0
openai>=1.40.0,<2.0.0
python-dotenv>=1.0.0
pypdf>=4.0.0
python-docx>=1.1.0
python-pptx>=1.0.0
pillow>=10.0.0
pytesseract>=0.3.10
PyMuPDF>=1.24.0
lxml>=4.9.0
textstat>=0.7.3
httpx>=0.27.0
# Phase 1 Computation Layer (verified STEM)
# ChemPy is NOT installed on Streamlit Cloud: it pulls pyodesys -> Jupyter/notebook
# and fails "Error installing requirements." Use SymPy balancer on Cloud; ChemPy stays in
# requirements-engines.txt for local/full installs.
sympy>=1.12
numpy>=1.26,<2.3
matplotlib>=3.8
schemdraw>=0.17
# Phase 2 Knowledge Layer (NCERT pilot + RAG)
chromadb>=0.5.0,<0.6.0
# Streamlit Cloud Linux needs a newer SQLite for Chroma
pysqlite3-binary; platform_system=="Linux"
# deploy rebuild: v3.3.3-drop-chempy-cloud-install
# Beta readiness - optional nicer PDF typography (fallback PDF always available)
fpdf2>=2.7.0,<3.0.0
