streamlit>=1.33.0,<2.0.0
openai>=1.40.0,<2.0.0
python-dotenv>=1.0.0
pypdf>=4.0.0
python-docx>=1.1.0
python-pptx>=1.0.0
pillow>=10.0.0
pytesseract>=0.3.10
PyMuPDF>=1.24.0
lxml>=4.9.0
textstat>=0.7.3
httpx>=0.27.0
# Phase 1 Computation Layer (verified STEM)
# ChemPy is NOT installed on Streamlit Cloud: it pulls pyodesys -> Jupyter/notebook
# and fails "Error installing requirements." Use SymPy balancer on Cloud; ChemPy stays in
# requirements-engines.txt for local/full installs.
sympy>=1.12
numpy>=1.26,<2.3
matplotlib>=3.8
schemdraw>=0.17
# Phase 2 Knowledge Layer — Chroma/RAG is OPTIONAL on Streamlit Cloud.
# chromadb + pysqlite3-binary hang/fail Cloud rebuilds ("app in the oven").
# Local/full installs: pip install -r requirements-engines.txt
# Runtime already falls back to keyword retrieval when Chroma is absent.
# deploy rebuild: v3.3.9-drop-chromadb-cloud-install
# Beta readiness - optional nicer PDF typography (fallback PDF always available)
fpdf2>=2.7.0,<3.0.0
