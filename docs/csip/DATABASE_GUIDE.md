# Database Guide

Metadata for relational databases, SQL concepts, ER models, normalisation, transactions, and queries.

- Schema viewer / ER diagram hooks for LXP.
- Reuses existing database engines where applicable; never invents query results.
- Assessment practices map to AME via coding/design-style hints (no answer leak).
