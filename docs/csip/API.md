# CSIP API

```python
from engines.computer_science_intelligence import (
    COMPUTER_SCIENCE_INTELLIGENCE_SMOKE_OK,
    analyse_computer_science_lesson,
    computer_science_quality_signals,
    pack_health,
    register_computer_science_pack,
)

assert COMPUTER_SCIENCE_INTELLIGENCE_SMOKE_OK
register_computer_science_pack(overwrite=True)
result = analyse_computer_science_lesson(uli)
signals = computer_science_quality_signals(uli)
health = pack_health()
```

Enrichment attaches under ULI subject intelligence via SIF; never mutates curriculum.
