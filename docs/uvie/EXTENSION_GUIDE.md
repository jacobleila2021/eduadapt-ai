# UVIE Extension Guide

1. Keep STEM draws on `engines.router` — never bypass.
2. Add organiser types under `providers/` with `invents_curriculum=False`.
3. Extend `PRIORITY_ORDER` only by appending below STEM/knowledge tiers.
4. Additive ULIQE seeds only (`ULIQE.UVIE.*`); never change certification thresholds.
5. Do not enable generative image providers in 1.x without provenance policy.
6. Next Platform Excellence phases: Premium Lesson Designer, Quality System, Rendering Engine.
