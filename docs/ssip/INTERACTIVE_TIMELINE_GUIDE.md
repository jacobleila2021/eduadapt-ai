# Interactive Timeline Guide

Module: `timelines.py`

Provides year-hint extraction from source text (when present), start/turning-point/outcome slots, and navigation hooks (`zoom`, `compare_periods`, `link_to_sources`).

`invents_events` is always `False`. Rendering: LXP.
